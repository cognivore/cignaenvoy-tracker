use flake
# If TMPDIR isn't /tmp, unset it
if [ "$TMPDIR" != "/tmp" ]; then
    unset TMPDIR
fi

# Cigna credentials path in passveil (used by start-dev.sh and LaunchAgent)
export PASSVEIL_CIGNA_PATH="customer.cignaenvoy.com/jons@geosurge.ai"
